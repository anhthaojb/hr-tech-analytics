# scrapy.cfg — đặt ở ROOT của project (cùng cấp với run_spiders.py)
# BUG 3 FIX: get_project_settings() tìm file này để biết SCRAPY_SETTINGS_MODULE.
# Thiếu file này → CrawlerProcess(get_project_settings()) trả về settings rỗng,
# mọi pipeline, middleware, spider đều không load được.

[settings]
default = jobscrapers.settings

[deploy]
project = jobscrapers
